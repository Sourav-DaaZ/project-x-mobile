const defaultValue = {
    debuggingMode: false,
    otpLength: 4,
    bookingStatus: ["requested", "confirmed", "rejected", "cancelled"],
    appVersion: {
        android: 1.0,
        ios: 1.0
    }
}

export default defaultValue;